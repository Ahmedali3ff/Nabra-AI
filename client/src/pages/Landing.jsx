// Voxena landing page — replaces the VoiceForge default landing.
// The original VoiceForge landing is preserved in git history.
import LandingPage from "../components/landing/LandingPage.jsx";

export default LandingPage;
